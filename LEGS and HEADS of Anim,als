legs=int(input("Enter the total no of legs:"))
heads=int(input("Enter the total no of hens:"))
f=0
for i in range(heads+1):
    cows=i
    hens=heads-cows
    r=(cows*4)+(hens*2)
    if r==legs:
        print("cows=",cows)
        print("hens=",hens)
        f=1
if f==0:
        print("solution not found")
